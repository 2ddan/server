/**
 * buff公式表
 */
// =================================== 导入
import { Formula } from "app/mod/formula";

Formula.set("excitation_maxCount-event_count",function(max, min, pow,abs, f, t, card, scene, buff,af){return excitation_maxCount-event_count;});Formula.set("excitation_maxCount-event_count",function(max, min, pow,abs, f, t, card, scene, buff,af){return excitation_maxCount-event_count;});Formula.set("f.damaged>19",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.damaged>19;});Formula.set("f.hp<=f.maxHp*0.5",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.hp<=f.maxHp*0.5;});Formula.set("f.un_block>0",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.un_block>0;});Formula.set("f.vulnerability>0",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.vulnerability>0;});Formula.set("f.un_attack>0",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.un_attack>0;});Formula.set("f.cards_spread.type==1||f.cards_spread.type==3",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.cards_spread.type==1||f.cards_spread.type==3;});Formula.set("-card.cost_energy",function(max, min, pow,abs, f, t, card, scene, buff,af){return -card.cost_energy;});Formula.set("f.damage/3",function(max, min, pow,abs, f, t, card, scene, buff,af){return f.damage/3;});Formula.set("-1",function(max, min, pow,abs, f, t, card, scene, buff,af){return -1;});