/**
 * buff公式表
 */
// =================================== 导入
import { Formula } from "app/mod/formula";
Formula.set("{{number}}",function(number){return number;});Formula.set("{{number*10}}",function(number){return number*10;});