
    <input on-input="onInput" on-blur="onBlur" on-focus="onFocus"  autocomplete="new-{{it.type}}" class="put" style="top:0px;color:white" type="{{it.type}}" maxlength="{{it.length}}" placeholder="{{it.placeholder?it.placeholder:''}}" value="{{it.text?it.text:''}}" />
  