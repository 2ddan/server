
{
    /* server listen port */
    "port":80,
    /* mongodb connect url */
    "dbpath":"mongodb://localhost:29817/test",
    /* upside port corresponding to the directory */
    "static":"../static/",
    "log":"../../log/",
    "router":{
        /* static directory default page */
        "root":["index.html"],
        /* error page */
        "error":{
            "404":"error/404.html"
        }
    },
    "hotfix":{
        /* The directorys which don't participate in hotfix */
        "without":["boot/"]
    }
}